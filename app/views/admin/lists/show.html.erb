<h1>Admin::Lists#show</h1>管理
<p>Find me in app/views/admin/lists/show.html.erb</p>
投稿詳細

  <p>ユーザーネーム
  <%= @list.user.name %></p>

  <p>タイトル</p>
  <span><%= @list.title %></span>

  <h2>画像</h2>
  <%= image_tag @list.get_image, :size => '300x300' %>

  <p>材料</p>
  <span><%= @list.material %></span>
  <p>本文</p>
  <span><%= @list.body %></span>

<div>
  <p>コメント件数：<%= @list.post_comments.count %></p>
  <% @list.post_comments.each do |post_comment| %>
    <%= post_comment.user.name %>
    <%= post_comment.created_at.strftime('%Y/%m/%d') %><%= post_comment.comment %>
      <%= link_to "削除", admin_list_post_comment_path(post_comment.list, post_comment), method: :delete %>
  <% end %>

